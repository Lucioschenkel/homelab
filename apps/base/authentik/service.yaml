apiVersion: v1
kind: Service
metadata:
  name: authentik
spec:
  ports:
  - port: 9000
  selector:
    app.kubernetes.io/component: server
    app.kubernetes.io/instance: authentik
  type: ClusterIP

